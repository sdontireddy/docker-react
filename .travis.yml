sudo: required
services:
    - docker

before_install:
    - docker build -t sdontireddy/docker-react -f Dockerfile.Dev .
    
script:
    - docker run sdontireddy/docker-react npm run test -- --coverage
